# ESP32 AudioNode PC app. Install: python -m pip install -r requirements.txt
# Minimum versions, not a reproducible lockfile.
numpy>=1.24
imageio-ffmpeg>=0.6
Flask>=3.0
Flask-SocketIO>=5.3.6
eventlet>=0.35
simple-websocket>=1.0
requests>=2.28

# Optional Windows loopback: python -m pip install PyAudioWPatch
