[build-system]
requires = ["setuptools>=74", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "esp32-audio-node"
dynamic = ["version"]
description = "ESP32-S3 Wi-Fi speaker firmware plus a browser-based Python player that streams 48 kHz mono audio as RTP L16 over UDP, with node management and a 10-band equalizer."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Nadeem" }]
requires-python = ">=3.11"
keywords = ["esp32", "esp32s3", "esp-idf", "i2s", "max98357a", "rtp", "udp",
            "audio-streaming", "wifi-speaker", "flask", "equalizer"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Web Environment",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: C",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Topic :: Multimedia :: Sound/Audio :: Players",
]
dependencies = [
    # Must match requirements.txt and audio_player/deps.py. The self-check
    # fails if these three lists drift apart.
    "Flask>=3.0",
    "Flask-SocketIO>=5.3.6",
    "imageio-ffmpeg>=0.6",
    "simple-websocket>=1.0",
]

# Only for the Windows loopback mode of audio_player/send_pcm.py. Neither
# numpy nor PyAudioWPatch is imported by the server or the web app.
optional-dependencies = {windows_loopback = ["numpy>=1.24", "pyaudiowpatch"]}

[project.scripts]
audio-player = "audio_player.app:main"

[tool.setuptools.dynamic]
version = {attr = "audio_player.__version__"}

[tool.setuptools.packages.find]
where = ["."]
include = ["audio_player*"]

[tool.setuptools.package-data]
audio_player = ["templates/**/*", "static/**/*"]

[tool.setuptools]
include-package-data = true
