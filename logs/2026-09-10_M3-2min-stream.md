# 2026-09-10 — M3 2-minute acceptance run (data path)

## Goal
Close M3 acceptance: ≥2 min dropout-free streaming, prompt stop at stream end.

## Test setup
- No firmware changes — board already running committed M3 build (2735bfe).
- Harness: `tmp/server_test.ps1` — 120 s of 1 kHz tone @ vol 0.5 via `server/send_pcm.py stream 1234 120 1000 0.5`.
- Monitor started FIRST (`--no-reset`), server after → ghost-connection fix retained.

## New board quirk discovered + fix (logged in PROJECT_STATE §5 #12)
- Board sat in `rst:0x15 USB_UART_CHIP_RESET, boot:0x0 DOWNLOAD → "waiting for download"`; every `idf.py monitor` open re-reset it into download mode.
- Fix (no unplug needed): with COM5 free, run
  `python -m esptool --chip esp32s3 -p COM5 run`  (stub + hard reset → app runs)
  then attach with `idf.py -p COM5 monitor --no-reset`.
- Harness updated to: esptool run FIRST → then monitor --no-reset → then server.

## Results (monitor_m0.log, server_out.log)
- Server: sent 11,520,000 bytes in 120.0 s — full session, clean exit.
- Board: CONNECTED t=18518 ms → stream end (11520000 bytes) t=138532 ms → exactly 120.0 s.
- Ring-full drops: 0. recv errors: 0. PSRAM ring stable entire run.
- Post-stream retries (errno=104) are expected — server had already exited; board retries every 2 s.
- Board WiFi RSSI -41 dBm.

## Status
- M3 DATA PATH: ✅ verified for full 2 min, zero loss.
- M3 AUDIO: awaiting user confirmation (tone heard 120 s, then prompt silence <500 ms after end).
